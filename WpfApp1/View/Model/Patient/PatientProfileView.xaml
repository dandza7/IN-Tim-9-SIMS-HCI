<Page x:Class="WpfApp1.View.Model.Patient.PatientProfileView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.View.Model"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PatientProfileView">

    <Grid>
        <Button Style="{StaticResource LogoutButton}"/>
        <Label Content="MyProfile" FontSize="30" FontWeight="ExtraBlack" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TabControl Style="{StaticResource PatientTabControl}" Height="600" Width="600">
            <TabItem Header="Information" Background="{StaticResource LightestBlueBrush}" Foreground="{StaticResource DarkBlueBrush}">
                <Grid Background="{StaticResource LightBlueBrush}"/>
            </TabItem>
            <TabItem Header="Therapies">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Reports">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Notifications">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
